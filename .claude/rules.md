# Правила работы с проектом

## 1. НЕ ДАВАТЬ ОЦЕНКИ ВРЕМЕНИ

**НИКОГДА не писать оценки времени типа:**
- ❌ "+2-3 часа"
- ❌ "займёт 1-1.5 часа"
- ❌ "это быстро, 30 минут"

**Почему:** Пользователь работает С АГЕНТОМ, а не один. Оценки для "ручной работы" вводят в заблуждение и раздражают.

**Исключение:** Только в правиле работы с ошибками (где время диагностики критично для выбора).

## 2. Принцип работы с ошибками

При любой ошибке ассистент ОБЯЗАН предложить выбор между двумя вариантами:

❌ Ошибка: [краткое описание]

А) РАЗОБРАТЬСЯ (время: ~X мин)
Плюсы: [главное преимущество]
Риски если НЕ разберёмся: [что может сломаться потом]

Б) ОБОЙТИ (время: ~X мин)

Плюсы: [главное преимущество]
Риски: [технический долг, будущие проблемы]

💡 Рекомендую: [А/Б] потому что [обоснование]

Выбираешь?



**Исключения:** критические ситуации (безопасность, потеря данных) - сразу копаем глубже.

**Критерий рекомендации А (разобраться):**
- Ошибка в фундаментальном инструменте (npm, git, node)
- Может повлиять на будущую работу
- Время диагностики оправдано

**Критерий рекомендации Б (обойти):**
- Проблема изолирована
- Обходной путь общепринят
- Диагностика займёт слишком много времени

## 3. Проверка Virtual Environment перед установкой зависимостей

**ВСЕГДА перед командой `pip install` проверять наличие venv.**

**Как проверить:**
- Посмотреть есть ли папка `.venv/` или `venv/` в проекте
- Проверить активирован ли venv (в терминале должен быть префикс `(.venv)`)

**Если venv НЕ создан:**

Предложить пользователю:

```
⚠️ Virtual environment не создан

Рекомендую создать venv для изоляции зависимостей проекта.

**Зачем:**
- Разные проекты могут требовать разные версии библиотек
- requirements.txt будет чистым (только зависимости MOTL)
- На проде также используется venv - одинаковое окружение

**Создать venv?**
```

**Создание venv:**
```bash
python -m venv .venv
```

**Активация:**
- Windows: `.venv\Scripts\activate`
- Linux/Mac: `source .venv/bin/activate`

**Важно:** После создания venv пользователю может понадобиться переоткрыть терминал или VSCode автоматически предложит это
