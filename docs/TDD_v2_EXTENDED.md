# Technical Design Document: Telegram Bot Settings Viewer

> ⚠️ **EXTENDED VERSION (v2) - Потенциальная доработка**
>
> Этот документ описывает **расширенную версию** с дополнительными возможностями:
> - ✨ Мобильная адаптация (responsive design, tabs layout)
> - ✨ Обработка изображений (cropping, resize, compression)
> - ✨ Сложная валидация и автоматическая оптимизация файлов
>
> **Текущая реализация:** Desktop-only версия **БЕЗ** этих функций (см. `TechnicalDesignDocument.md`)
>
> Этот документ сохранен для будущих доработок и reference.

---

## 1. Описание продукта и целевая задача

### 1.1 Контекст

Разработка Telegram-ботов для конференций, выставок и ярмарок требует сбора контента от клиентов: текстов описаний, изображений профиля, приветственных сообщений и других настроек бота. Клиенты не имеют технического опыта работы с BotFather и не понимают требований Telegram к форматам данных.

### 1.2 Проблема

Текущий процесс согласования контента включает:
- Отправку текстового ТЗ клиенту с требованиями (лимиты символов, размеры изображений)
- Получение некорректных данных (слишком длинные тексты, неподходящие форматы изображений)
- Множественные итерации переписки для исправления
- Непонимание клиентом разницы между различными элементами UI бота (short_description vs description, профиль vs стартовое сообщение)

**Затраты времени**: часы на каждого клиента для объяснения и исправления данных.

### 1.3 Решение

**Telegram Bot Settings Viewer** - веб-приложение для сбора, валидации и preview контента настроек Telegram-бота.

**Основные функции:**
1. **Интерактивная форма** - структурированный ввод всех необходимых данных (тексты, изображения)
2. **Live Preview** - визуализация в реальном времени, как настройки будут выглядеть в Telegram
3. **Автоматическая валидация** - проверка соответствия требованиям Telegram (длина текста, размеры изображений)
4. **Обработка изображений** - автоматическое сжатие и оптимизация
5. **Экспорт** - выгрузка готового архива с корректными данными для последующей настройки бота

### 1.4 Целевая аудитория

- **Основная**: клиенты студии (организаторы мероприятий, маркетологи) - нетехнические специалисты
- **Вторичная**: внутренние менеджеры проектов для контроля качества данных

### 1.5 Scope проекта

**Включено в MVP:**
- Обязательные поля: название бота, short_description, description, about, аватар
- Опциональные элементы: одна Inline-кнопка под приветственным сообщением с текстом кнопки и ответным сообщением
- Валидация данных по требованиям Telegram API
- Preview интерфейса для мобильной версии Telegram
- Экспорт в ZIP (JSON с данными + папка с изображениями)
- Локальное сохранение прогресса (IndexedDB)

**Явно исключено:**
- Регистрация и авторизация пользователей
- База данных и серверное хранение
- Прямая интеграция с Telegram Bot API или BotFather
- Конструктор логики бота (меню, команды, сценарии)
- Настройка inline-кнопок, клавиатур, webhook
- Мультиязычность (только русский интерфейс)

**Обоснование ограничений:**
- Инструмент решает узкую задачу согласования контента, а не создания бота
- Отсутствие backend упрощает разработку и эксплуатацию (нет затрат на хостинг, безопасность данных)
- Ограничение одной опциональной кнопкой предотвращает scope creep

---

## 2. Архитектурный подход

### 2.1 Выбор платформы: Веб-приложение (SPA)

**Решение:** Single Page Application, доступное по URL.

**Альтернативы и причины отказа:**

| Вариант | Плюсы | Минусы | Причина отказа |
|---------|-------|--------|----------------|
| Telegram Bot (диалоговый интерфейс) | Клиент не покидает Telegram | Неудобная загрузка файлов, нет визуального preview, линейный UX | Невозможность показать live preview, неудобство работы с множественными полями |
| Telegram Mini App (Web App внутри Telegram) | Нативное ощущение, открывается в Telegram | Ограниченное пространство экрана, усложнённая работа с файлами (особенно iOS) | Для заполнения "ТЗ" удобнее полноэкранный браузер на компьютере, drag & drop |
| Desktop приложение (Electron) | Оффлайн работа | Необходимость установки, поддержка разных ОС | Излишняя сложность, клиенты не будут устанавливать ПО для разовой задачи |

**Итоговое решение:** Веб-сайт (SPA) с возможностью открыть на любом устройстве через браузер.

### 2.2 Архитектура: Client-Side Only

**Решение:** Вся логика работает на клиенте (в браузере), без серверной части.

**Технические решения:**
- **State management**: хранение состояния формы в памяти браузера (React state)
- **Persistence**: автоматическое сохранение в IndexedDB браузера (через idb-keyval)
- **Обработка файлов**: все операции (валидация, сжатие, превью) выполняются в браузере
- **Генерация архива**: создание ZIP файла на клиенте с помощью JSZip
- **Скачивание**: использование File API браузера для сохранения архива

**Обоснование:**
- Нет затрат на backend-инфраструктуру (хостинг, база данных, API)
- Не требуется защита персональных данных (нет серверного хранения)
- Простота развёртывания (статический хостинг: Vercel/Netlify/GitHub Pages)
- Мгновенная работа без сетевых задержек
- Масштабируемость "из коробки" (CDN)

**Недостатки и митигация:**
- **Потеря данных при закрытии вкладки** → IndexedDB с автосохранением каждые 2-5 секунд (хранит File objects напрямую, гигабайтные лимиты)
- **Нет централизованного сбора** → Клиент скачивает архив и отправляет вручную (приемлемо для бизнес-процесса)

### 2.3 UI/UX паттерн: Split View с Live Preview

**Макет экрана:**
```
┌─────────────────────────────────────────────────┐
│  Заголовок + инструкции                          │
├──────────────────────┬──────────────────────────┤
│                      │                          │
│   Форма ввода        │    Preview макет         │
│   (левая часть)      │    (правая часть)        │
│                      │                          │
│   [поля]             │   ┌──────────────────┐   │
│   [загрузка файлов]  │   │  Мобильный       │   │
│   [валидация]        │   │  телефон         │   │
│                      │   │  с интерфейсом   │   │
│                      │   │  Telegram        │   │
│                      │   └──────────────────┘   │
│                      │                          │
├──────────────────────┴──────────────────────────┤
│  [Сохранить черновик] [Скачать архив]           │
└─────────────────────────────────────────────────┘
```

**На мобильных устройствах (< 768px):**
- Вкладки (Tabs): "Заполнение" и "Превью"
- Переключение между формой и preview
- Сохранение изменений при переключении вкладок

**Обоснование desktop split view:**
- Мгновенная обратная связь - клиент видит результат изменений
- Снижение когнитивной нагрузки - не нужно "представлять" как выглядит
- Выявление ошибок до отправки данных

**Обоснование табов на мобильных:**
- Виртуальная клавиатура занимает ~40% экрана, оставляя мало места для split view
- Табы предотвращают конкуренцию за пространство экрана
- Пользователь может сфокусироваться на заполнении, затем проверить preview

---

## 3. Технологический стек

### 3.1 Frontend Framework: React 18 + TypeScript

**Обоснование выбора React:**
- Компонентная архитектура идеальна для реактивного preview
- Огромная экосистема готовых библиотек (формы, валидация, работа с файлами)
- Виртуальный DOM эффективен для частых обновлений preview
- Большое комьюнити = проще найти решения проблем

**Обоснование TypeScript:**
- Type safety предотвращает ошибки в структуре данных (критично для валидации)
- Автодополнение IDE ускоряет разработку
- Самодокументирование кода через типы

**Отклонённые альтернативы:**

| Framework | Почему не выбран |
|-----------|------------------|
| Vue 3 | Меньше готовых UI-библиотек для сложных форм и file handling |
| Svelte | Меньшее комьюнити, меньше библиотек для работы с изображениями |
| Vanilla JS | Сложность управления состоянием для live preview, больше boilerplate кода |
| Next.js | Избыточность SSR/SSG для client-only приложения, усложнённая настройка |

### 3.2 Build Tool: Vite

**Обоснование:**
- Мгновенный холодный старт dev-сервера
- Hot Module Replacement (HMR) быстрее Webpack
- Простая конфигурация из коробки для React + TS
- Оптимизированная сборка для production (Rollup)

**Альтернатива:** Create React App - устаревший, медленный, больше не поддерживается активно.

### 3.3 Styling: Tailwind CSS 3

**Обоснование:**
- Utility-first подход ускоряет вёрстку
- Не нужно писать CSS-файлы
- Встроенная адаптивность (responsive design)
- Консистентность дизайна через design tokens

**Отклонённые альтернативы:**
- **CSS Modules** - больше boilerplate, медленнее итерации
- **Styled Components** - runtime overhead, избыточность для простого UI
- **MUI/Ant Design** - тяжеловесные UI-киты, сложно кастомизировать под Telegram-like дизайн

### 3.4 Ключевые библиотеки

| Библиотека | Назначение | Обоснование |
|------------|------------|-------------|
| **React Hook Form** | Управление формой | Минимальные ре-рендеры, встроенная валидация, удобная работа с файлами |
| **Zod** | Схема валидации | Type-safe валидация, переиспользуемые схемы, понятные сообщения об ошибках |
| **idb-keyval** | Хранение черновиков | Простой API как у localStorage, но хранит File/Blob напрямую, гигабайтные лимиты (<1KB размер) |
| **react-dropzone** | Drag & Drop файлов | Проверенное решение, поддержка всех браузеров, удобный API |
| **react-image-crop** | Обрезка изображений | Клиентская обрезка под требуемое соотношение сторон |
| **browser-image-compression** | Сжатие изображений | Работает в браузере (без backend), сохраняет качество |
| **JSZip** | Создание ZIP-архива | Клиентская генерация архивов, поддержка всех браузеров |
| **file-saver** | Скачивание файлов | Кроссбраузерное сохранение Blob как файла |

**Примечание:** State management через React Hook Form internal state. Zustand можно добавить позже при проблемах с производительностью.

### 3.5 Deployment: Vercel (или Netlify)

**Обоснование:**
- Бесплатный тариф для небольших проектов
- Автоматический деплой из Git (push в main → деплой в production)
- Встроенный CDN
- HTTPS из коробки
- Поддержка custom domain

**Альтернатива:** GitHub Pages - бесплатно, но без функций preview для PR и медленнее настройка.

---

## 4. Структура данных

### 4.1 Telegram Bot Settings Schema

```typescript
interface BotSettings {
  // Обязательные поля
  botName: string;              // Имя бота (длина: 5-32 символа, только латиница, цифры, _)
  shortDescription: string;     // Короткое описание (до 120 символов)
  description: string;          // Описание (до 512 символов)
  about: string;                // О боте в профиле (до 120 символов)
  avatar: File | null;          // Аватар (640x640px, до 5MB, JPG/PNG)

  // Опциональные поля
  welcomeInlineButton?: {
    enabled: boolean;
    text: string;               // Текст Inline-кнопки (до 64 символов)
    response: string;           // Статический текст ответа бота (до 4096 символов)
  };
}

**Примечание:**
- welcomeInlineButton - это Inline Keyboard кнопка, прикреплённая к приветственному сообщению бота (под текстом, а не внизу экрана как Reply Keyboard)
- `response` - статический текст, который бот автоматически пришлёт при нажатии кнопки (требует реализации callback handler на backend)

**⚠️ ВАЖНО - Граница ответственности инструмента:**
Инструмент **формирует только контент** для кнопки, но **не создаёт логику бота**. Для работы кнопки разработчик должен реализовать callback handler на backend, который будет:
- Ловить нажатие на кнопку (callback_query)
- Отправлять текст из поля `response` в ответ

Клиент должен понимать: "зелёная форма" ≠ "кнопка работает". Форма проверяет только валидность контента.
```

### 4.2 Валидационные правила (Zod Schema)

```typescript
const botSettingsSchema = z.object({
  botName: z.string()
    .min(5, "Минимум 5 символов")
    .max(32, "Максимум 32 символа")
    .regex(/^[a-zA-Z0-9_]+$/, "Только латиница, цифры и _")
    .refine(val => val.toLowerCase().endsWith('bot'),
      "Имя бота должно заканчиваться на 'bot'"),

  // UI-предупреждение под полем botName (реализовать в компоненте):
  // "ℹ️ Проверяется только формат имени. Доступность проверяется в @BotFather при регистрации."

  shortDescription: z.string()
    .max(120, "Максимум 120 символов"),

  description: z.string()
    .max(512, "Максимум 512 символов"),

  about: z.string()
    .max(120, "Максимум 120 символов"),

  avatar: z.custom<File>()
    .refine(file => file.size <= 5 * 1024 * 1024, "Максимум 5MB")
    .refine(file => ['image/jpeg', 'image/png'].includes(file.type), "Только JPG или PNG"),

  welcomeInlineButton: z.object({
    enabled: z.boolean(),
    text: z.string().max(64).optional(),
    response: z.string().max(4096).optional()
  }).optional()
});
```

### 4.3 Формат выходного архива

```
bot-settings.zip
├── data.json          # JSON с текстовыми данными
└── images/
    └── avatar.jpg     # Сжатое и оптимизированное изображение
```

**Структура data.json:**
```json
{
  "botName": "example_bot",
  "shortDescription": "Текст короткого описания",
  "description": "Полное описание бота...",
  "about": "О боте в профиле",
  "welcomeInlineButton": {
    "text": "О выставке",
    "response": "Текст статического ответа бота..."
  }
}
```

---

## 5. Критические технические решения

### 5.1 Обработка изображений на клиенте

**Проблема:** Клиенты загружают изображения в произвольных форматах и размерах (например, 15MB PNG с разрешением 4K, прямоугольные логотипы 16:9).

**Решение - многошаговый pipeline:**

#### Шаг 1: Валидация при загрузке
- Проверка типа файла (MIME type: image/jpeg, image/png)
- Проверка исходного размера (<20MB для удобства обработки)
- Загрузка в canvas для анализа

#### Шаг 2: Cropping UI (для не-квадратных изображений)
**Проблема:** Автоматический resize прямоугольного изображения в квадрат приводит к:
- Искажениям (squash)
- Потере критичных частей (center crop)
- Непрофессиональному виду (letterboxing)

**Решение:**
```typescript
const aspectRatio = width / height;
const isSquare = aspectRatio > 0.9 && aspectRatio < 1.1;

if (!isSquare) {
  // Открыть модальное окно с react-image-crop
  // Пользователь выбирает область 1:1
  // Preview результата в реальном времени
}
```

- **Если изображение квадратное** (~1:1) → пропустить cropping, сразу к сжатию
- **Если не квадратное** → открыть UI с `react-image-crop`:
  - Модальное окно с интерактивным выбором области
  - Фиксированное соотношение сторон 1:1
  - Preview обрезанного результата
  - Кнопка "Готово" → сохранить выбранную область

#### Шаг 3: Проверка прозрачности (PNG)
**Проблема:** Конвертация PNG с прозрачностью (альфа-канал) в JPEG заменяет прозрачность на чёрный/белый фон, что портит логотипы.

**Решение:**
```typescript
function hasTransparency(imageData: ImageData): boolean {
  for (let i = 3; i < imageData.data.length; i += 4) {
    if (imageData.data[i] < 255) return true;
  }
  return false;
}

if (file.type === 'image/png' && hasTransparency(imageData)) {
  // Оставить PNG, сжать до приемлемого размера
  const compressed = await compressPNG(file);
  if (compressed.size > 5MB) {
    throw new Error("PNG с прозрачностью слишком большой. Упростите изображение.");
  }
  return compressed;
} else {
  // Конвертировать в JPEG
  return compressToJPEG(file, 0.85);
}
```

#### Шаг 4: Resize и сжатие
- Resize до 640x640px (достаточно для аватаров Telegram)
- Сжатие:
  - **JPEG** с качеством 85% (баланс качества/размера)
  - **PNG** (если есть прозрачность) с оптимизацией
- Финальная проверка размера (<5MB)

#### Шаг 5: Preview в реальном времени
- Показ обработанного изображения в макете Telegram (круглая обрезка)
- Индикатор: "Обработано: 3.2 MB → 450 KB"
- Предупреждение, если итоговый размер всё ещё большой

**Библиотеки:**
- `react-image-crop` - UI для выбора области обрезки
- `browser-image-compression` - сжатие (использует Web Workers)

**Обработка ошибок и тайм-ауты:**
- **Тайм-аут обработки:** 30 секунд на весь pipeline
  ```typescript
  const timeout = setTimeout(() => {
    throw new Error("Обработка слишком долгая. Упростите изображение или уменьшите разрешение.");
  }, 30000);

  try {
    await processImage(file);
  } finally {
    clearTimeout(timeout);
  }
  ```
- PNG с прозрачностью >5MB → "Упростите изображение или сохраните в JPEG с белым фоном"
- Слишком большой исходный файл (>20MB) → "Уменьшите разрешение перед загрузкой"
- Некорректный формат → "Поддерживаются только JPG и PNG"
- Тайм-аут превышен → "Обработка слишком долгая. Изображение слишком сложное для браузера"
- **Loading state:** Блокировка UI с индикатором прогресса во время обработки

### 5.2 Live Preview: Имитация UI Telegram

**Требования:**
- Точная визуализация как текст и изображения будут выглядеть в Telegram
- Поддержка тёмной и светлой темы
- Отображение обрезки текста (ellipsis) при превышении лимита
- Мобильный вид (основной кейс использования ботов)
- **КРИТИЧНО:** Показать все три контекста, где используются разные поля

**Реализация - три preview компонента:**

1. **`ChatListItem` - Список контактов (для short_description)**
   - **Проблема:** `short_description` (120 символов) НЕ показывается в профиле или чате
   - Отображается только:
     - В списке контактов (как превью последнего сообщения)
     - В ссылке приглашения `t.me/botname`
   - **Компонент должен показывать:**
     ```
     [Аватар] Bot Name
              short_description (с ellipsis при переполнении)
     ```
   - Без этого preview клиент не поймёт разницу между `short_description` и `description`

2. **`BotProfile` - Экран профиля бота (для about, description)**
   - Показывает:
     - Аватар (полноразмерный круг)
     - Имя бота
     - `about` - текст "О боте" в профиле (120 символов)
     - `description` - полное описание (512 символов)

3. **`ChatStart` - Стартовый экран чата (для приветствия + inline кнопки)**
   - Показывает:
     - Приветственное сообщение (использует `description`)
     - Inline-кнопка (если enabled) под сообщением
     - **⚠️ Визуальный индикатор на кнопке:** "Требует разработки" (бейдж или затемнение)
     - Preview текста ответа при клике на кнопку

**Вёрстка с нуля** (не использовать готовые UI-киты типа "Telegram UI React")
  - Причина: готовые решения либо устаревшие, либо избыточные
  - Создать компоненты: `TelegramPreview`, `ChatListItem`, `BotProfile`, `ChatStart`

- **Референсы:** скриншоты реального Telegram UI (iOS/Android) для точности отступов и шрифтов

- **CSS переменные** для тем:
  ```css
  :root[data-theme="light"] {
    --tg-bg: #ffffff;
    --tg-text: #000000;
    --tg-secondary: #707579;
  }

  :root[data-theme="dark"] {
    --tg-bg: #212121;
    --tg-text: #ffffff;
    --tg-secondary: #aaaaaa;
  }
  ```

- **Disclaimer и UI-подписи:**
  - Крупное предупреждение над preview:
    > "Схематичное отображение. Реальные шрифты и отступы зависят от устройства пользователя (iOS/Android)."
  - Маленькая подпись под preview:
    > "📱 Пример: Android (стиль может отличаться на iOS)"
  - Это снижает ожидания pixel-perfect соответствия

**Обоснование custom вёрстки vs готовые решения:**
- Полный контроль над внешним видом
- Минимальный размер бандла (только нужные компоненты)
- Простота поддержки при изменениях Telegram UI

### 5.3 Сохранение прогресса: IndexedDB Strategy (idb-keyval)

**Проблема:** Клиент случайно закрывает вкладку, теряя 30 минут работы.

**Решение через idb-keyval:**

**Почему IndexedDB, а не localStorage:**
- **Лимиты:** localStorage ограничен ~5MB на домен (~2.5MB полезных данных), IndexedDB - гигабайты
- **Хранение File objects:** localStorage не хранит File/Blob напрямую, требуется base64 (+33% overhead), IndexedDB хранит нативно
- **Простота:** библиотека `idb-keyval` (<1KB) даёт API как у localStorage, но асинхронный
- **Надёжность:** QuotaExceededError в localStorage может сломать сохранение даже текстовых полей

**Реализация:**

1. **Проверка поддержки IndexedDB (КРИТИЧНО):**
   ```typescript
   async function checkIDBSupport(): Promise<boolean> {
     try {
       await set('idb-test', 'test');
       await del('idb-test');
       return true;
     } catch (error) {
       console.error('IndexedDB not available', error);
       showWarning(
         "⚠️ Автосохранение недоступно. " +
         "Скачайте архив после заполнения, чтобы не потерять данные."
       );
       return false;
     }
   }
   ```
   - Проверка при загрузке приложения
   - Если IDB недоступен (Private mode, Safari ограничения) → показать предупреждение
   - Приложение продолжает работать, но без автосохранения

2. **Автоматическое сохранение:**
   - Debounced сохранение при каждом изменении (задержка 2 секунды)
   - Использование `set('tg-bot-draft', formData)` где formData включает File objects
   - Никакой конвертации в base64 - File хранится напрямую
   - **Toast при первом успешном сохранении:**
     ```
     ✓ Черновик сохранён в браузере
     ⚠️ В режиме инкогнито/приватном режиме данные могут быть удалены
     ```

3. **Восстановление при загрузке:**
   - `get('tg-bot-draft')` при монтировании компонента
   - Модальное окно: "Найден несохранённый черновик. Восстановить?"
   - Прямая загрузка File objects в форму

4. **Обработка ошибок:**
   - Try-catch при операциях с IndexedDB
   - Safari/iOS может агрессивно чистить IDB при low storage
   - Fallback сообщение пользователю при ошибках
   - Graceful degradation - приложение работает без persistence

5. **Ограничения и предупреждения:**
   - Safari (особенно iOS): может удалить данные при нехватке места
   - Private/Incognito mode: IDB может быть ограничен или очищен при закрытии
   - Предупреждать пользователя мягко, чтобы не пугать, но информировать

4. **Ручное управление:**
   - Кнопка "Сохранить черновик" для явного сохранения
   - Кнопка "Очистить форму" с подтверждением (также очищает IndexedDB)
   - Индикатор времени последнего автосохранения

**Пример кода с защитой от race condition:**
```typescript
import { get, set, del } from 'idb-keyval';
import { useForm } from 'react-hook-form';

function BotSettingsForm() {
  const [isHydrating, setIsHydrating] = useState(true);
  const { reset, ...form } = useForm();

  // Восстановление при загрузке - КРИТИЧНО для предотвращения гонки состояний
  useEffect(() => {
    async function loadDraft() {
      try {
        const draft = await get('tg-bot-draft');
        if (draft) {
          reset(draft); // Загружаем черновик только после чтения IDB
        }
      } catch (error) {
        console.error('Failed to load draft', error);
      } finally {
        setIsHydrating(false); // Разблокируем форму
      }
    }
    loadDraft();
  }, [reset]);

  // Блокируем ввод до завершения загрузки черновика
  if (isHydrating) {
    return <Spinner text="Загрузка черновика..." />;
  }

  return <FormContent {...form} />;
}

// Сохранение (включая File objects)
async function saveDraft(formData) {
  await set('tg-bot-draft', {
    ...formData,
    avatar: fileObject,  // File object напрямую
    timestamp: Date.now()
  });
}
```

**Важность isHydrating state:**
Без него возникает race condition:
- t=0ms: Форма рендерится пустой (IDB читается асинхронно)
- t=50ms: Пользователь начинает печатать
- t=100ms: Прилетают данные из IDB → `reset()` стирает ввод пользователя

**Обоснование выбора:**
- Простая интеграция (API идентичен localStorage)
- Решает критичную проблему лимитов
- Нет overhead на base64 конвертацию
- Поддержка всех современных браузеров

### 5.4 Генерация и скачивание архива

**Процесс:**
1. Валидация всех полей (Zod schema)
2. Формирование объекта данных (без File objects)
3. Создание ZIP с помощью JSZip:
   ```typescript
   const zip = new JSZip();
   zip.file("data.json", JSON.stringify(data, null, 2));
   zip.file("images/avatar.jpg", avatarBlob);
   ```
4. Генерация Blob
5. Скачивание через `file-saver`:
   ```typescript
   // Фиксированное имя для безопасности (не использовать botName)
   saveAs(blob, "telegram-bot-config.zip");
   ```

**Почему фиксированное имя:**
- `botName` вводит пользователь - может содержать:
  - Зарезервированные имена Windows (CON, PRN, AUX)
  - Path traversal символы (../../)
  - Пробелы и эмодзи
- Валидация должна отсечь проблемные значения, но безопаснее использовать статичное имя
- Имя бота содержится внутри JSON - менеджер увидит его после распаковки

**Обработка ошибок:**
- Невалидные данные → подсветка ошибок, скролл к первой ошибке
- Слишком большой архив (>10MB) → предупреждение, но разрешить скачивание
- Ошибка генерации ZIP → сообщение об ошибке с предложением сохранить данные как JSON

---

## 6. План реализации по этапам

### Этап 1: Визуальный фундамент (Visual Foundation)
**Цель:** Получить работающий визуальный прототип с базовой интерактивностью.

**Задачи:**
1. Инициализация проекта:
   - Создание Vite + React + TypeScript проекта
   - Настройка Tailwind CSS
   - Настройка ESLint + Prettier
   - Установка базовых зависимостей: `idb-keyval`, `react-hook-form`, `zod`

2. Вёрстка макета страницы:
   - Двухколоночный layout (форма + preview) для desktop
   - Табы для мобильных устройств (< 768px): "Заполнение" и "Превью"

3. Создание компонентов Telegram Preview:
   - `TelegramPhone` - обёртка в виде мобильного устройства
   - `ChatListItem` - элемент списка контактов (показывает short_description)
   - `BotProfile` - экран профиля бота (показывает about + description)
   - `ChatStart` - стартовый экран чата (показывает приветствие + inline кнопку)

4. Реализация 2-3 базовых полей ввода:
   - Bot Name
   - Short Description
   - Description (с пояснением: "используется в профиле и как текст приветствия")

5. Live update:
   - Связь React state с компонентами preview
   - Реактивное обновление при вводе текста

**Результат:** Работающая страница, где при вводе текста в форму он появляется в preview Telegram-макета.

**Обоснование первого этапа:**
- Визуальный результат мотивирует продолжать
- Доказывает жизнеспособность концепции
- Позволяет получить обратную связь от коллег/клиентов
- Visual-first подход критичен для preview-инструмента (в отличие от обычных CRUD-приложений)

---

### Этап 2: Валидация и обратная связь (Validation & Feedback)
**Цель:** Добавить систему валидации и визуальные индикаторы ошибок.

**Задачи:**
1. Интеграция React Hook Form:
   - Настройка формы с контроллерами
   - Подключение валидации

2. Создание Zod-схем валидации:
   - Правила для всех текстовых полей
   - Обязательная проверка: имя бота должно заканчиваться на 'bot'
   - Кастомные сообщения об ошибках на русском

3. UI компоненты для валидации:
   - `InputField` с индикаторами ошибок (красная обводка)
   - Счётчик символов под полем (например: "45 / 120")
   - Цветовая индикация: серый (норма) → жёлтый (близко к лимиту) → красный (превышение)
   - **Под полем botName:** мелкий текст "ℹ️ Проверяется формат. Доступность - в @BotFather"

4. Визуализация валидации в preview:
   - Отображение ellipsis (...) при обрезке текста
   - Highlighting проблемных элементов

**Результат:** Форма с полноценной валидацией, предотвращающая ввод некорректных данных.

**Обоснование:**
- Валидация - core value инструмента (предотвращение ошибок клиента)
- Реализуется после визуальной части, так как нужен UI для отображения ошибок
- Zod + React Hook Form = industry standard, проверенная связка

---

### Этап 3: Работа с изображениями (Image Handling)
**Цель:** Полноценная загрузка, обработка и preview изображений с обрезкой.

**Задачи:**
1. Компонент загрузки изображения:
   - Drag & Drop зона (`react-dropzone`)
   - Click to upload
   - Preview загруженного изображения
   - Кнопка удаления/замены

2. Валидация изображений (Zod custom validators):
   - Тип файла (JPEG/PNG)
   - Размер файла (<20MB исходный)
   - Загрузка в canvas для анализа

3. **Cropping UI (`react-image-crop`):**
   - Определение соотношения сторон (aspect ratio)
   - Если изображение не квадратное (не ~1:1):
     - Открыть модальное окно с cropper
     - Фиксированное соотношение 1:1
     - Preview результата обрезки
     - "Готово" → сохранить выбранную область
   - Если квадратное → пропустить cropping

4. **Проверка прозрачности PNG:**
   - Анализ альфа-канала через canvas ImageData
   - Если PNG имеет прозрачность:
     - Оставить PNG, сжать оптимально
     - Если >5MB → ошибка с предложением упростить
   - Если нет прозрачности или JPEG:
     - Конвертировать в JPEG (качество 85%)

5. **Resize и сжатие:**
   - Интеграция `browser-image-compression`
   - Resize до 640x640px
   - Финальная проверка размера (<5MB)
   - Индикатор: "Обработано: 3.2 MB → 450 KB"

6. Отображение в preview:
   - Аватар во всех preview компонентах (круглая обрезка)
   - Корректное позиционирование в UI Telegram

7. Обработка ошибок и производительность:
   - PNG с прозрачностью >5MB → "Упростите изображение"
   - Слишком большой файл >20MB → "Уменьшите разрешение"
   - Некорректный формат → "Только JPG и PNG"
   - **Тайм-аут 30 секунд** на весь pipeline обработки
   - Если превышен → "Обработка слишком долгая. Изображение слишком сложное"
   - **Блокировка UI** с индикатором прогресса во время обработки
   - Индикатор: "Обрабатываем... (это может занять до 30 сек)"

**Результат:** Полный pipeline обработки изображений с cropping UI, проверкой прозрачности и сжатием.

**Обоснование:**
- Работа с изображениями сложнее текста (асинхронность, обработка, валидация)
- Реализуется отдельным этапом для изоляции сложности
- Cropping UI критичен - автоматический resize прямоугольных изображений приводит к искажениям
- Проверка прозрачности предотвращает испорченные логотипы при конвертации PNG→JPEG

---

### Этап 4: Полный набор полей и опциональная функциональность
**Цель:** Завершить все поля ввода согласно scope проекта.

**Задачи:**
1. Добавление оставшихся полей:
   - About (текст в профиле бота)
   - Любые другие обязательные поля согласно Telegram Bot API

2. Реализация опциональной Inline-кнопки:
   - Checkbox "Добавить приветственную Inline-кнопку"
   - **⚠️ Предупреждение над секцией:**
     ```
     Инструмент формирует только контент.
     Для работы кнопки требуется разработка бота на backend.
     ```
   - Условный рендеринг полей:
     - Текст кнопки (до 64 символов)
     - Текст статического ответа бота (до 4096 символов)
   - Уточнение в UI: "Текст, который бот автоматически пришлёт в ответ"
   - Валидация опциональных полей
   - Preview Inline-кнопки под приветственным сообщением (не внизу экрана)
   - **В preview:** визуальный бейдж на кнопке "Требует разработки" или затемнение

3. Переключатель темы:
   - Toggle для переключения Light/Dark темы preview
   - Применение CSS-переменных для изменения цветов макета

4. Disclaimer и инструкции:
   - Заголовок с кратким описанием инструмента
   - Предупреждение над preview: "Схематичное отображение..."
   - Tooltips/подсказки для полей (при наведении)

**Результат:** Полная форма со всеми необходимыми полями и опциональной функциональностью.

**Обоснование:**
- Расширение функциональности после проверки базовых механик
- Опциональная кнопка - важная бизнес-фича, но не критична для MVP proof-of-concept
- Disclaimer решает проблему ожиданий клиентов относительно точности preview

---

### Этап 5: Persistence и черновики (IndexedDB через idb-keyval)
**Цель:** Защита от потери данных при случайном закрытии страницы.

**Задачи:**
1. **Проверка поддержки IndexedDB при загрузке (КРИТИЧНО):**
   - Попытка записи/чтения тестового значения
   - Если IDB недоступен (Private mode, Safari ограничения):
     - Показать предупреждение: "⚠️ Автосохранение недоступно. Скачайте архив после заполнения"
     - Продолжить работу без автосохранения
   - Toast при первом успешном сохранении:
     ```
     ✓ Черновик сохранён в браузере
     ⚠️ В режиме инкогнито данные могут быть удалены
     ```

2. Утилиты для работы с IndexedDB:
   - Обёртка над `idb-keyval` для save/load/clear операций
   - Хранение File objects напрямую (без base64 конвертации)
   - Обработка ошибок с graceful degradation
   - Предупреждение про Safari/iOS ограничения

3. **Защита от race condition (КРИТИЧНО):**
   - Добавить `isHydrating` state
   - При монтировании компонента:
     - Установить `isHydrating = true`
     - Читать из IndexedDB асинхронно
     - Только после завершения чтения: `isHydrating = false`
   - Пока `isHydrating === true`:
     - Показывать spinner/скелетон ИЛИ
     - Блокировать инпуты формы (disabled)
   - Это предотвращает стирание ввода пользователя при загрузке черновика

4. Автоматическое сохранение:
   - Подписка на изменения формы
   - Debounced сохранение (задержка 2 сек)
   - Индикатор "Сохранено" (исчезает после изменений)

5. Восстановление при загрузке:
   - Проверка IndexedDB при монтировании компонента
   - Модальное окно с предложением восстановить (опционально)
   - Прямая загрузка данных и File objects в форму через `reset()`

6. Ручное управление:
   - Кнопка "Сохранить черновик" (явное сохранение)
   - Кнопка "Очистить форму" (с подтверждением, очищает IndexedDB)
   - Отображение времени последнего сохранения

**Результат:** Надёжная защита от потери данных с поддержкой больших файлов.

**Обоснование:**
- Критично для UX - клиент может потратить 30+ минут на заполнение
- IndexedDB решает проблему лимитов localStorage (~5MB → гигабайты)
- `idb-keyval` так же прост как localStorage, но без его ограничений
- Реализуется после всех полей, чтобы точно знать структуру данных

---

### Этап 6: Экспорт и финализация (Export & Polish)
**Цель:** Итоговая функциональность - генерация и скачивание архива.

**Задачи:**
1. Генерация JSON:
   - Сборка объекта данных из формы
   - Форматирование JSON (pretty-print)

2. Создание ZIP-архива:
   - Интеграция JSZip
   - Добавление data.json
   - Добавление папки images/ с обработанным аватаром
   - Генерация имени архива (timestamp + bot name)

3. Компонент кнопки экспорта:
   - "Скачать архив" (disabled если есть ошибки валидации)
   - Индикатор процесса генерации
   - Обработка ошибок (с fallback на скачивание JSON)

4. Финальная валидация перед экспортом:
   - Проверка всех обязательных полей
   - Скролл к первой ошибке, если валидация не прошла
   - Summary модальное окно: "Архив готов к скачиванию. Содержит: X файлов, размер: Y MB"

5. Полировка UI:
   - Анимации и transitions
   - Loading states
   - Error boundaries
   - Accessibility (a11y): keyboard navigation, ARIA labels

**Результат:** Полностью функциональное приложение с экспортом готового архива.

**Обоснование:**
- Экспорт - финальная точка workflow, реализуется в конце
- Все предыдущие этапы необходимы для корректной работы экспорта
- Полировка в конце, когда функциональность стабильна

---

## 7. Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Telegram изменит требования к форматам | Средняя | Высокое | Вынести валидационные правила в конфиг (constants.ts), легко обновлять |
| Preview не совпадает с реальным UI | Высокая | Среднее | Добавить крупный disclaimer, фокус на структуре, а не пиксель-перфекте |
| Клиенты загружают огромные файлы (>50MB) | Средняя | Среднее | Жёсткий лимит на размер исходного файла (<20MB), сообщение об ошибке |
| Scope creep (запросы на доп. функции) | Высокая | Высокое | Чёткая документация scope, отказ от расширения до завершения MVP |
| Браузер не поддерживает IndexedDB | Очень низкая | Среднее | Graceful degradation: приложение работает без автосохранения, предупреждение пользователю |
| Клиент путает Inline и Reply кнопки | Средняя | Низкое | Чёткое название "Inline-кнопка" в UI, визуализация в preview под сообщением |
| Race condition при загрузке черновика | Высокая | Высокое | `isHydrating` state блокирует форму до завершения загрузки из IDB |
| Автоматический resize искажает прямоугольные изображения | Высокая | Высокое | Cropping UI (react-image-crop) для ручного выбора области 1:1 |
| Конвертация PNG с прозрачностью в JPEG портит логотипы | Средняя | Высокое | Проверка альфа-канала, сохранение PNG при наличии прозрачности |
| Клиент не понимает разницу short_description vs description | Высокая | Среднее | Отдельный preview компонент ChatListItem для short_description |
| Клиент ожидает что inline-кнопка работает автоматически | Высокая | Среднее | Явное предупреждение "Требует разработки" в UI и бейдж в preview |
| IndexedDB недоступен (Safari/Incognito) - потеря черновика | Средняя | Среднее | Проверка поддержки, предупреждение, graceful degradation |
| Обработка большого изображения подвешивает UI | Средняя | Среднее | Тайм-аут 30 сек, блокировка UI с индикатором прогресса |
| Клиент ожидает что botName доступен если валидация зелёная | Высокая | Низкое | Предупреждение: "Проверяется формат. Доступность - в @BotFather" |

---

## 8. Метрики успеха

**Технические метрики:**
- Размер бандла: <500KB (gzipped)
- Время первой отрисовки (FCP): <1.5s
- Поддержка браузеров: Chrome/Firefox/Safari последние 2 версии

**Бизнес-метрики:**
- Сокращение времени согласования контента с клиентом с ~3 часов до <30 минут
- Уменьшение количества итераций переписки с 5-7 до 1-2
- 100% корректность данных от клиентов (проходят валидацию инструмента)

**Качественные метрики:**
- Клиенты самостоятельно заполняют форму без дополнительных объяснений
- Менеджеры получают готовый архив, сразу пригодный для настройки бота
- Внутренняя команда использует инструмент для всех новых проектов

---

## 9. Дальнейшее развитие (Post-MVP)

**Потенциальные улучшения** (явно НЕ включены в текущий scope):

### Приоритет 1 (Быстрые улучшения):
1. **HD аватары (1024x1024)** - опция для High-DPI экранов (iPhone Pro Max, Samsung Ultra)
2. **README.txt в архиве** - инструкция для менеджера, что делать с файлами
3. **Расширенные метаданные** - добавить в data.json поля: `generatedAt`, `toolVersion`, `clientUserAgent`
4. **Zustand для state management** - если возникнут проблемы с производительностью ререндеров

### Приоритет 2 (Функциональные расширения):
5. **Telegram Mini App версия** - после валидации веб-версии, для более нативного опыта
6. **Дополнительные элементы бота:** bot commands, меню, дополнительные inline-кнопки
7. **Мультиязычность** - интерфейс на английском, поля для разных языков контента
8. **Templates/Presets** - шаблоны для типичных конференций/выставок

### Приоритет 3 (Серьёзные изменения архитектуры):
9. **Прямая интеграция с BotFather** - автоматическая настройка через Telegram Bot API (требует backend)
10. **Аналитика использования** - сбор анонимной телеметрии для улучшения UX
11. **Versioning** - поддержка нескольких версий настроек для одного бота

**Критерий для расширения scope:** Завершение MVP, использование в 10+ реальных проектах, сбор обратной связи.

---

## 10. Заключение

**Telegram Bot Settings Viewer** решает конкретную проблему согласования контента через:
- **Визуализацию** - клиент видит результат до отправки
- **Валидацию** - невозможно отправить некорректные данные
- **Автоматизацию** - обработка изображений без ручной работы

Выбранный стек (**React + TypeScript + Tailwind + Client-Side Architecture**) оптимален для:
- Быстрой разработки MVP
- Нулевых операционных затрат
- Простоты поддержки и развития

План реализации построен по принципу **visual-first**: сначала работающий интерфейс, затем наращивание функциональности. Это позволяет получить обратную связь на ранних этапах и подтвердить ценность продукта.

**Следующий шаг:** Инициализация проекта и начало Этапа 1 (Visual Foundation).
