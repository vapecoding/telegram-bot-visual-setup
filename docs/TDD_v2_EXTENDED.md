# Technical Design Document: Telegram Bot Visual Setup

> **CURRENT STATUS: MVP COMPLETED**
>
> Desktop-only версия полностью функциональна. Все основные функции реализованы.
>
> Документ актуализирован: 2026-01-18

---

## 1. Описание продукта и целевая задача

### 1.1 Контекст

Разработка Telegram-ботов для конференций, выставок и ярмарок требует сбора контента от клиентов: текстов описаний, изображений профиля, приветственных сообщений и других настроек бота. Клиенты не имеют технического опыта работы с BotFather и не понимают требований Telegram к форматам данных.

### 1.2 Проблема

Текущий процесс согласования контента включает:
- Отправку текстового ТЗ клиенту с требованиями (лимиты символов, размеры изображений)
- Получение некорректных данных (слишком длинные тексты, неподходящие форматы изображений)
- Множественные итерации переписки для исправления
- Непонимание клиентом разницы между различными элементами UI бота (short_description vs description, профиль vs стартовое сообщение)

### 1.3 Решение

**Telegram Bot Visual Setup** - веб-приложение для сбора, валидации и preview контента настроек Telegram-бота.

**Реализованные функции:**
1. **Интерактивная форма** - структурированный ввод всех необходимых данных
2. **Live Preview** - визуализация в реальном времени в трёх режимах (Список чатов, Профиль, Диалог)
3. **Bidirectional Highlighting** - подсветка связанных элементов при наведении/фокусе (форма ↔ превью)
4. **Валидация** - проверка соответствия требованиям Telegram с визуальной индикацией
5. **Экспорт** - выгрузка готового ZIP-архива с данными и изображениями
6. **Автосохранение** - сохранение черновика в IndexedDB браузера

### 1.4 Scope проекта (Реализовано)

**Обязательные поля:**
- Имя бота (Display Name) - до 64 символов
- Username - 5-32 символа, заканчивается на "bot"
- Короткое описание (short_description) - до 120 символов
- Описание (description) - до 512 символов
- О боте (about) - до 120 символов
- Аватар - квадратное изображение 640x640px+, до 5MB, JPG/PNG
- Description Picture (BotPic) - строго 640x360px, до 5MB, JPG/PNG
- Privacy Policy URL - до 256 символов

**Опциональные поля:**
- Первое сообщение бота (firstMessageText) - до 4096 символов
- Inline-кнопка под сообщением (inlineButtonText) - до 64 символов
- Ответ на нажатие кнопки (inlineButtonResponse) - до 4096 символов

**Явно исключено:**
- Регистрация и авторизация
- Backend и серверное хранение
- Мобильная адаптация (пока desktop-only)
- Dark theme toggle
- Cropping UI для изображений
- Автоматическое сжатие изображений

---

## 2. Архитектура

### 2.1 Client-Side Only SPA

Вся логика работает в браузере без серверной части.

**Технические решения:**
- **State management**: React useState/useCallback hooks
- **Persistence**: IndexedDB через idb-keyval
- **Генерация архива**: JSZip + file-saver
- **Валидация**: Inline проверки + DownloadModal с FIELD_LIMITS

### 2.2 UI Layout: Split View

```
┌──────────────────────────────────────────────────────────┐
│  Заголовок: Telegram Bot Visual Setup                    │
├─────────────────────────┬────────────────────────────────┤
│                         │  [Режимы превью]               │
│   Форма ввода           │  📋 Список чатов               │
│   (левая часть,         │  👤 Профиль                    │
│    скроллится)          │  💬 Диалог                     │
│                         │                                │
│   [Поля ввода]          │  ┌────────────────────┐        │
│   [Счётчики символов]   │  │   iPhone frame     │        │
│   [Загрузка файлов]     │  │   с Telegram UI    │        │
│                         │  └────────────────────┘        │
│                         │                                │
│                         │  [FieldHelp блок]              │
│                         │  [📦 Скачать архив]            │
├─────────────────────────┴────────────────────────────────┤
│  [Демо данные] [Очистить]                                │
└──────────────────────────────────────────────────────────┘
```

---

## 3. Технологический стек

### 3.1 Реализованный стек

| Технология | Версия | Назначение |
|------------|--------|------------|
| **React** | 19.x | UI framework |
| **TypeScript** | 5.9.x | Type safety |
| **Vite** | 7.x | Build tool + dev server |
| **Tailwind CSS** | 4.x | Styling (utility-first) |
| **idb-keyval** | 6.x | IndexedDB persistence |
| **JSZip** | 3.x | ZIP-архив генерация |
| **file-saver** | 2.x | Скачивание файлов |
| **react-dropzone** | 14.x | Drag & Drop загрузка файлов |

### 3.2 Особенности Tailwind CSS v4

Tailwind v4 имеет баг с CSS nesting - генерирует пустые `&:hover { }` блоки. Решение: кастомные CSS-классы в `src/index.css` с традиционными `:hover` селекторами и `!important`.

```css
/* Пример workaround */
.btn-download:hover {
  background-color: #22c55e !important;
  transform: scale(1.05) !important;
}
```

---

## 4. Структура данных

### 4.1 Form Data Interface

```typescript
interface FormData {
  // Обязательные поля
  botName: string;              // Display name (до 64 символов)
  username: string;             // @username_bot (5-32 символа)
  shortDescription: string;     // Короткое описание (до 120)
  description: string;          // Полное описание (до 512)
  about: string;                // О боте в профиле (до 120)
  privacyPolicyUrl: string;     // Privacy Policy URL (до 256)

  // Изображения
  avatarUrl: string | null;     // Data URL аватара
  botPicUrl: string | null;     // Data URL Description Picture

  // Опциональные - диалог
  firstMessageText: string;     // Первое сообщение (до 4096)
  inlineButtonText: string;     // Текст кнопки (до 64)
  inlineButtonResponse: string; // Ответ на кнопку (до 4096)
}
```

### 4.2 Лимиты символов (FIELD_LIMITS)

```typescript
const FIELD_LIMITS = {
  botName: 64,
  shortDescription: 120,
  description: 512,
  about: 120,
  username: 32,
  privacyPolicyUrl: 256,
  firstMessageText: 4096,
  inlineButtonText: 64,
  inlineButtonResponse: 4096,
} as const;
```

### 4.3 Формат выходного архива

```
telegram-bot-config.zip
├── data.json          # JSON с текстовыми данными
└── images/
    ├── avatar.jpg     # Аватар (если загружен)
    └── botpic.jpg     # Description Picture (если загружен)
```

---

## 5. Компоненты

### 5.1 Структура файлов

```
src/
├── App.tsx                    # Главный компонент, форма
├── index.css                  # Tailwind + кастомные стили
├── components/
│   ├── AvatarUpload.tsx       # Загрузка аватара с валидацией
│   ├── BotPicUpload.tsx       # Загрузка Description Picture
│   ├── DownloadModal.tsx      # Модалка валидации перед скачиванием
│   ├── FieldHelp.tsx          # Блок справки по полям
│   └── preview/
│       ├── TelegramPhone.tsx  # Обёртка iPhone + режимы
│       ├── ChatListItem.tsx   # Превью списка чатов
│       ├── BotProfile.tsx     # Превью профиля бота
│       ├── ChatStart.tsx      # Превью стартового экрана
│       └── FirstMessage.tsx   # Превью диалога после START
├── utils/
│   └── zipExport.ts           # Генерация ZIP-архива
└── hooks/
    └── useAutoSave.ts         # Хук автосохранения в IndexedDB
```

### 5.2 Preview компоненты

| Компонент | Показывает | Поля формы |
|-----------|------------|------------|
| `ChatListItem` | Список контактов | botName, shortDescription, avatar |
| `BotProfile` | Профиль бота | botName, username, about, privacyPolicyUrl, avatar |
| `ChatStart` | Стартовый экран | botName, description, avatar, botPic |
| `FirstMessage` | Диалог после START | firstMessageText, inlineButtonText, inlineButtonResponse, avatar, botPic |

### 5.3 Bidirectional Highlighting

При фокусе/наведении на поле формы → подсветка соответствующего элемента в превью.
При наведении на элемент превью → подсветка соответствующего поля формы.

CSS-эффекты в `index.css`:
- `highlight-pulse-shadow` - фоновая пульсация
- `highlight-pulse-light` - свечение текста на тёмном фоне
- `highlight-avatar-pulse` - кольцевая пульсация для аватара
- `highlight-pic-pulse` - пульсация для Description Picture
- `animate-marquee` - бегущая строка для длинных имён

---

## 6. Валидация

### 6.1 Real-time валидация в форме

- **Счётчик символов**: серый (норма) → жёлтый (>80% лимита) → красный (превышение)
- **Граница поля**: серая → красная при превышении лимита
- **Разрешено вставлять больше символов** - клиент может подготовить контент в другом месте

### 6.2 Валидация при скачивании (DownloadModal)

**Ошибки (блокируют скачивание):**
- Превышение лимита символов любого поля
- Пустые обязательные поля (botName, username)
- Username короче 5 символов или не заканчивается на "bot"
- Некорректный аватар (не квадратный)

**Предупреждения (не блокируют):**
- Пустые опциональные поля
- Аватар не квадратный (будет обрезан Telegram)
- Отсутствует Description Picture

### 6.3 Валидация изображений

**Аватар:**
- Формат: JPEG, PNG
- Размер файла: до 5MB
- Разрешение: минимум 640x640px, максимум 4096x4096px
- Соотношение сторон: ~1:1 (±5%), иначе предупреждение

**Description Picture (BotPic):**
- Формат: JPEG, PNG
- Размер файла: до 5MB
- Разрешение: строго 640x360px

---

## 7. Persistence (IndexedDB)

### 7.1 Автосохранение

- Debounced сохранение каждые 2 секунды при изменениях
- Хранение в IndexedDB через `idb-keyval`
- File objects (изображения) хранятся как Data URL

### 7.2 Восстановление

- При загрузке страницы проверяется наличие черновика
- Данные восстанавливаются автоматически
- Изображения восстанавливаются из Data URL

### 7.3 Graceful Degradation

- При недоступности IndexedDB (Private mode) - предупреждение
- Приложение продолжает работать без автосохранения

---

## 8. Реализованные UX-функции

### 8.1 Marquee для длинных имён

Для имён бота длиннее 20 символов включается бегущая строка в `BotProfile`:
```typescript
const needsMarquee = botName.length > 20;
```

### 8.2 Интерактивный превью диалога

- Нажатие START → показ первого сообщения
- Нажатие inline-кнопки → показ ответа
- "Липкие" состояния при наведении на поля диалога

### 8.3 Автоскролл в превью

При фокусе на поле формы превью автоматически прокручивается к соответствующему элементу.

### 8.4 Демо-данные

Кнопка "Демо данные" заполняет форму примерами для быстрого тестирования.

---

## 9. Потенциальные улучшения (Post-MVP)

### Приоритет 1 (Быстрые улучшения):
- [ ] Dark theme toggle для превью
- [ ] Мобильная адаптация (tabs layout)
- [ ] README.txt в архиве с инструкциями

### Приоритет 2 (Функциональные расширения):
- [ ] Cropping UI для не-квадратных изображений
- [ ] Автоматическое сжатие больших изображений
- [ ] Telegram Mini App версия

### Приоритет 3 (Архитектурные):
- [ ] Zustand для state management
- [ ] Zod schema валидация
- [ ] E2E тесты (Playwright)

---

## 10. Share Feature (Supabase)

### 10.1 Архитектура

Функция "Поделиться" позволяет создать публичную ссылку на конфигурацию (действует 7 дней).

**Backend:** Supabase (Free tier)
- **База:** PostgreSQL с RLS
- **Storage:** Supabase Storage для картинок
- **Доступ:** Только через RPC-функции (без прямого SELECT)

### 10.2 Схема данных

Таблица `shared_configs`:
- `id` UUID (PK)
- `secret_hash` TEXT (хеш секрета, не сам секрет)
- `payload` JSONB (тексты/настройки, URL картинок)
- `created_at` TIMESTAMPTZ
- `expires_at` TIMESTAMPTZ (now() + 7 days)

### 10.3 RPC-функции

**`share_create(payload_json)`**
1. Валидация payload (размер, обязательные поля)
2. Генерация `id` (UUID) и `secret` (случайная строка)
3. Сохранение `secret_hash = hash(secret)`
4. Возврат `{id, secret}`

**`share_get(share_id, share_secret)`**
1. Поиск по `id`
2. Проверка `expires_at > now()` и совпадение хеша
3. Возврат `payload` или ошибка

### 10.4 Формат ссылки

```
https://tgvisual.ru/#share=<id>.<secret>
```

Fragment (`#`) не отправляется на сервер — меньше следов в логах.

### 10.5 Flow

**Создание:**
1. Загрузка картинок в Supabase Storage → получение URL
2. Сборка payload с URL картинок
3. Вызов `share_create(payload)`
4. Формирование ссылки `#share=id.secret`

**Открытие:**
1. Парсинг `#share=id.secret` при загрузке
2. Вызов `share_get(id, secret)`
3. Восстановление формы из payload
4. Если истекло — сообщение "Ссылка истекла"

### 10.6 Лимиты

- **Дневной лимит:** 5 ссылок на пользователя (localStorage)
- **TTL:** 7 дней
- **Размер payload:** ~200KB max

### 10.7 Free tier риски

- Проект может "засыпать" при низкой активности (~7 дней)
- Решение: heartbeat через GitHub Actions 2 раза в неделю
- 90 дней на восстановление paused проекта

---

## 11. Deployment

**Текущий статус:** Production (tgvisual.ru)

**Хостинг:** GitHub Pages + Cloudflare (основной), GitHub Pages (зеркало)

**Build команды:**
```bash
npm run dev      # Development server
npm run build    # Production build
npm run preview  # Preview production build
```

---

## 12. Changelog

### 2026-01-18
- Добавлена валидация лимитов в DownloadModal
- Разрешено вставлять больше символов чем лимит
- Восстановлена marquee анимация для длинных имён
- Фиксированный размер шрифта text-xl для имени бота

### 2026-01-17
- Добавлены hover-эффекты для всех интерактивных элементов
- Bidirectional highlighting (form ↔ preview)
- Автоскролл превью при фокусе на поля
- ZIP-экспорт с валидацией
- FieldHelp компонент

### Ранее
- Базовая форма и превью
- IndexedDB persistence
- Загрузка аватара и BotPic
- Три режима превью
